# Test-only explicit dynamic route proving the private upstream contract.
location = /edge-test-runtime {
    proxy_pass http://confenge_web_runtime;
    include /etc/nginx/confenge-public-edge/runtime-proxy.conf;
    include /etc/confenge/web/current/20-security-headers.conf;
}
